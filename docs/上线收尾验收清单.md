# 上线收尾验收清单

本文记录正式上线前的收尾检查项。每次功能调整后，如影响主管、员工流程或上线规则，需要同步更新本文档和《主管和员工使用说明》。

## 一、全流程验收

当前自动化测试覆盖并已通过：

- 员工提交链上流水批注后进入待审核。
- 主管审核通过后进入正式业务统计。
- 主管驳回后，员工可修改并重新提交。
- 已审核批注可提交修正版本，修正审核通过后新版本生效。
- 已审核批注可提交取消入账版本，取消入账审核通过后不再计入业务收支。
- 非业务流水可由主管标记，并可恢复为待批注。
- 凭证图片仅允许图片类型，支持压缩和权限下载校验。
- 钱包纳入管理起始时间可控制历史流水是否默认需要批注。
- 内部划转两侧流水可识别为一笔业务记录，且不计入入账、出账统计。
- 链上流水排序和待处理状态优先级正常。
- 主管提交租用续费交易哈希后，系统按哈希排重并校验续费。
- 未开通租户按首次开通优惠价付款时，可以自动开通；平台收款钱包可在主管端一键复制。
- 租用金额异常时进入平台收入列表，由管理员人工处理。
- 管理员可按月、按天或月数加天数给租户手工续费。
- 到期租户可自动停用，续费后可自动启用。
- 多租户数据视图隔离正常。
- 员工查看全部账目权限生效。
- 工单中心权限正常：主管只看本系统工单，管理员工单中心不显示演示账号工单。
- 演示账号领取、重复领取、登录后当天不再分配、演示数据重置规则正常。

最近一次完整测试结果：

- 命令：`node --test`
- 结果：`76/76` 通过

## 二、权限和可见性

已覆盖：

- 员工不可审核批注。
- 员工无账号管理、系统管理、服务器管理权限。
- 未勾选查看全部账目时，员工只能看到自己的批注记录和待处理流水。
- 主管只能看到本系统数据。
- 管理员可查看全局数据。
- 管理员工单中心只展示正式租户工单，不展示演示账号工单。
- 主管看不到管理员的查看级操作日志，例如查看凭证、导出、手动查询等。
- 非管理员看不到平台收入列表和线下手工续费入口。

上线前仍建议人工复核：

- 分别用管理员、主管、员工账号登录，确认左侧菜单和页面入口符合预期。
- 用员工账号确认不能进入审核中心、账号管理、租用管理、系统管理、服务器管理。
- 用主管账号确认只能看到本系统钱包、流水、员工和日志。

## 三、正式登录方式

当前状态：

- 生产服务已启用 `APP_ENV=production`。
- 默认测试密码已关闭，登录页不再显示测试密码提示。
- 正式登录使用登录账号、密码；已绑定登录密钥的账号还需要 6 位动态验证码。
- 演示账号不需要登录密钥，通过 `/demo` 领取后可直接进入演示系统。
- `NODE_ENV=production` 仅用于服务运行环境，正式登录模式以 `APP_ENV=production` 为准。

上线前需要完成：

- 确认管理员、主管、员工都使用正式密码并已妥善保存登录密钥。
- 确认“我的账号”中修改密码、重置登录密钥流程可用。

## 四、备份和恢复

当前状态：

- 服务器 `usdt-ledger-backup.timer` 已启用，每 6 小时执行一次备份，保留 14 天。
- 最近备份 sha256 校验通过。
- 已使用最近 PostgreSQL 备份恢复到临时库 `usdt_ledger_restore_test` 并验证可查询，随后已删除临时库。
- 已验证附件凭证压缩包可正常读取。

上线前还需要完成：

- 确认是否需要把 `.env` 关键配置纳入离线备份或密码管理器备份。

建议备份内容：

- PostgreSQL 数据库。
- `/opt/usdt-ledger/data` 或实际附件目录。
- `/opt/usdt-ledger/.env`。
- 当前部署包或 Git 版本。

## 五、安全检查

当前状态：

- 服务器公网监听端口为 `22`、`80`、`443`。
- PostgreSQL 仅监听 `127.0.0.1:5432` 和 `[::1]:5432`，未对公网开放。
- 应用仅监听 `127.0.0.1:5173`，由 Nginx 反向代理。
- `.env` 权限为 `600`，归属 `ledgerapp:ledgerapp`。
- HTTPS 正常，Nginx 配置校验通过。
- 已启用 HSTS、CSP、`X-Frame-Options: DENY`、`X-Content-Type-Options: nosniff` 等响应头。
- 测试已覆盖上传图片类型限制、管理员查看级日志不向主管暴露等权限边界。

上线前还需要完成：

- 确认正式账号密码已交付并可登录。
- 最终确认 TRON API Key 只存在服务器环境变量中，不进入前端和公开仓库。
- 最终确认附件只通过权限接口访问，不暴露静态目录。

## 六、正式初始配置和数据清理

当前状态：

- 已在清理前执行并校验备份：`20260609T122932Z`。
- 已在清理后执行并校验干净状态备份：`20260609T123205Z`。
- 正式租户业务测试数据已清空：正式租户钱包、链上流水、批注、往来款、平账、工单均为 0。
- 演示账号配置已保留，并已重新生成标准演示数据。
- 附件目录无旧测试凭证文件残留。
- 平台收款钱包地址为空，月租费用当前为 100 USDT。
- 服务器已启用 `APP_ENV=production`，默认测试密码不再生效。

上线前需要确认：

- 正式平台收款钱包地址。
- 正式月租费用。
- 首次开通优惠价，如无活动则设置为 0。
- 正式管理员账号和密码。
- 初始租户名单。
- 每个租户的主管账号。

需要用户提供或最终确认：

- 正式平台收款钱包地址。
- 正式月租费用。
- 正式管理员账号命名和密码策略。
